import Card from "../organism/CardColumn"

function PropsCard(){
    const props = [
        {
            name:"",

        }
    ]
}

return (
    <div>
        {PropsCard.map((PropsCard) => {
            return <Card name={}/>
        })}
    </div>
)

export default PropsCard;